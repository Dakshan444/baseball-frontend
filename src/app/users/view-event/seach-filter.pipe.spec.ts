import { SeachFilterPipe } from './seach-filter.pipe';

describe('SeachFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SeachFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
