import { VenueSearchPipe } from './venue-search.pipe';

describe('VenueSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VenueSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
